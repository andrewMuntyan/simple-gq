# import * from './generated/prisma.graphql'

type Mutation {
  createCategory(name: String!): Category!
  createWord(content: String!, category: CategoryCreateOneInput!): Word!
}
type Query {
  categories: [Category!]!
  words: [Word!]!
}
